<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; padding: 40px; }
        
        /* Style cho từng slide */
        .slide {
            border: 2px solid #333;
            border-radius: 10px;
            padding: 30px;
            margin-bottom: 50px; /* Khoảng cách để dễ nhìn khi preview */
            page-break-after: always; /* QUAN TRỌNG: Ngắt trang khi in PDF */
            height: 90vh; /* Chiều cao full trang giấy */
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        h1 { color: #2563eb; font-size: 36px; text-align: center; }
        h2 { color: #1e40af; border-bottom: 2px solid #e5e7eb; padding-bottom: 10px; }
        ul { font-size: 24px; line-height: 1.6; }
        .note { 
            margin-top: auto; 
            padding: 15px; 
            background: #f3f4f6; 
            border-left: 4px solid #9ca3af;
            font-size: 16px; 
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="slide" style="text-align: center; align-items: center;">
        <h1 style="font-size: 60px;"><%= title %></h1>
        <p>Generated by LectureGen AI</p>
    </div>

    <% slides.forEach(function(slide) { %>
        <div class="slide">
            <h2><%= slide.title %></h2>
            <p><%= slide.content %></p>
            <ul>
                <% slide.bullet_points.forEach(function(point) { %>
                    <li><%= point %></li>
                <% }); %>
            </ul>
            <% if (slide.note) { %>
                <div class="note">🗣️ Note: <%= slide.note %></div>
            <% } %>
        </div>
    <% }); %>
</body>
</html>
